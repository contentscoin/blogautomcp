---
name: naver-bc-automation
description: "네이버 블로그 자동 포스팅 파이프라인 (기획 -> 초안 -> 편집 -> 비주얼 디렉팅)"
---

# naver-bc-automation

이 스킬은 주어진 주제와 키워드를 바탕으로 네이버 블로그에 최적화된 고품질의 콘텐츠를 생성하고, 관련 이미지를 수집/생성하여, 자동으로 네이버 블로그에 포스팅(또는 예약 포스팅)을 진행하는 에이전틱 4단계 파이프라인을 실행합니다.

## 파이프라인 구조 (4단계 에이전틱 워크플로우)

1. **기획 스킬 (Planner)**: 주제와 키워드를 바탕으로 독자의 이목을 끄는 블로그 포스팅 기획안(제목, 스토리라인, 소주제)을 작성합니다.
2. **초안 작성 스킬 (Drafter)**: 기획안을 바탕으로 각 소주제에 해당하는 세부 본문 초안을 작성합니다.
3. **편집/모바일 최적화 스킬 (Editor)**: 모바일 환경에서 읽기 쉽도록 문장을 다듬고(50자 이내, 잦은 줄바꿈), 감성적인 어조와 이모지를 추가하며, 네이버 블로그 에디터의 특성(예: 물결표 사용 금지)을 반영하여 글을 고도화합니다.
4. **비주얼 디렉팅 스킬 (Visual Director)**: `ai-image-generation` 스킬과 연계하여, 각 단락의 내용과 분위기에 완벽하게 어울리는 사진을 찾기 위한 검색 키워드(searchKeyword)와 AI 이미지 생성을 위한 고품질 영어 프롬프트(imagePrompt)를 기획합니다.

## When to use

사용자가 다음과 같은 요청을 할 때 이 스킬을 활성화합니다:
- "블로그 포스팅 해줘"
- "주제 기반으로 자동 포스팅 실행해"
- "네이버 블로그에 글 써줘"
- "이 주제로 예약 글 발행해줘"
- "topic agent 실행해줘"

## Instructions

1. **사용자 요청 분석**: 사용자가 제시한 주제(`topic`), 추가 키워드(`keywords`), 카테고리(`type`), 발행 모드(`publishMode`), 예약 시간(`scheduledDate`)을 파악합니다.
2. **명령어 실행**: 파악된 인자를 바탕으로 다음 명령어를 실행합니다.
   ```bash
   npm run topic -- --type="[타입]" --topic="[주제]" --keywords="[키워드]" --publishMode="[publish/schedule/draft]" --scheduledDate="[YYYY-MM-DDTHH:MM]"
   ```
   *예시: `npm run topic -- --type="travel" --topic="다낭 골프 여행" --publishMode="schedule" --scheduledDate="2026-03-05T10:00"`*
3. **실행 모니터링**: 스크립트 실행 과정(1~4단계 파이프라인 진행 상황, 이미지 다운로드, 브라우저 자동화)을 모니터링하고 성공 여부를 사용자에게 보고합니다.
4. **문제 해결**: 에러 발생 시(특히 이미지 다운로드 에러, 네이버 로그인 세션 만료 등) 원인을 파악하고 사용자에게 해결 방법을 안내합니다.
